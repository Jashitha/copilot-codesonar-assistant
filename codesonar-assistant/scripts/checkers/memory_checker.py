"""
memory_checker.py  [STUB — not yet implemented]

Responsibility: detect potential memory management issues (leaks, use-after-
free, double-free, etc.).

Contract:
  - Accepts source code as a plain string.
  - Returns a list of finding dicts (empty until implemented).
  - Never prints, never reads files, never formats output.
"""


def run(code: str) -> list[dict]:
    """Placeholder — returns empty list until memory checks are implemented."""
    return []
